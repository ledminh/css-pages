// import { fromJS } from 'immutable';
// import { makeSelectCss2Domain } from '../selectors';

// const selector = makeSelectCss2Domain();

describe('makeSelectCss2Domain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
