// import { fromJS } from 'immutable';
// import { makeSelectCss4Domain } from '../selectors';

// const selector = makeSelectCss4Domain();

describe('makeSelectCss4Domain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
